plugins {
    id 'java'
}

group = rootProject.group
version = rootProject.version

repositories {
    mavenCentral()
}

dependencies {
    // Add collector-client dependencies here if/when needed
    implementation 'com.fasterxml.jackson.core:jackson-databind:2.21.1'
}